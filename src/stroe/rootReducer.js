import { combineReducers } from 'redux'
import { itemReducer } from './memo/actions'

// const rootReducer = combineReducers({
//   itemReducer,

// });
const rootReducer = combineReducers({
  itemReducer,
});
export default rootReducer